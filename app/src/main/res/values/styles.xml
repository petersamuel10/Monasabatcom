<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/bottom_nav_true</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/RatingDialogStyle</item>
        <item name="alertDialogStyle">@style/RatingDialogStyle</item>

    </style>

    <!--progress bar-->
    <style name="GeneralLinearProgress" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:progressBackgroundTint">@color/colorAccent</item>
    </style>

    <!-- Welcome page -->
    <style name="SplashActivityWithBGImageBackground" parent="AppTheme">
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/white</item>
        <item name="colorAccent">@android:color/white</item>
    </style>

    <style name="RatingBar" parent="@android:style/Widget.RatingBar">
        <item name="colorAccent">@color/orange</item>
        <item name="colorControlNormal">@color/orange</item>
        <item name="colorControlActivated">@color/golden</item>

        <item name="android:maxHeight">@dimen/_4sdp</item>
    </style>

    <style name="RatingDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">

        <item name="android:background">@android:color/white</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/RatingDialogButtonStyle</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/RatingDialogButtonStyle</item>
    </style>

    <style name="RatingDialogButtonStyle" parent="@style/Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/orange</item>
        <item name="android:textSize">@dimen/text_size_medium</item>
    </style>


    <style name="RatingDialogFadeAnim" parent="@style/Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Sugar.Switch.Affiliate" parent="Base.Widget.AppCompat.CompoundButton.Switch">
        <item name="colorSwitchThumbNormal">@android:color/white</item>
        <item name="colorAccent">@color/blue</item>
        <item name="android:color">@color/nav_color</item>
    </style>

    <style name = "MyDialog" parent="AppTheme" >
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name = "android:backgroundDimEnabled">true</item>
        <item name = "android:windowBackground" >@android:color/transparent</item >
    </style >

    <style name="PauseDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_down</item>
        <item name="android:windowExitAnimation">@anim/slide_up</item>
    </style>


</resources>
